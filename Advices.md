# Lyrien 创意建议书（Advices）

> 这份文档不是脑洞清单，而是我（AI 协作者）为 Lyrien 提创意的方法论 + 按优先级排好的具体提案。
> 每个提案都回答三个问题：**它吃的是什么数据？它强化哪条主线？它的代价是什么？**

---

## 一、我提创意的四条评估标准

Lyrien 已经不是"资讯推荐器"，而是一个开始积累数据的系统。所以好创意必须至少满足一条：

1. **数据飞轮**：让已有数据（feedback 流水、tag_snapshots、notes、review 成绩）产生新价值，
   或让新功能反过来喂饱旧数据。孤立的一次性功能不值钱。
2. **每日触点**：能嵌入"早看卡片 / 晚复习 / 周日周报"的既有节奏，而不是一个去了才有用的新页面。
3. **学习 > 资讯**：plan.md 的灵魂是"学习 ≠ 看过"。凡是以"看更多"为方向的创意降权，
   以"记得更牢、理解更深"为方向的加权。
4. **成本守恒**：LLM 调用必须有界。每日功能控制在几分钱量级，周期功能（周报级）放宽。

反面清单（提了就扣分）：需要新重型基础设施的、为他人而不是为你设计的、
把系统推向"又一个 RSS 阅读器"的。

---

## 二、第一梯队：数据飞轮类（我最推荐的方向）

### 2.1 掌握度感知推荐（从"懂你兴趣"到"懂你水平"）⭐ 最高优先级
- **吃什么数据**：`feedback` 表里已经攒下的 `review:remember/fuzzy/forgot` 流水 + `cards.tags`。
- **做什么**：按标签统计复习正确率，得到"掌握度地图"。把它和兴趣权重一起写进
  `prompts/select.md`：「用户对 X 兴趣高但复习正确率只有 40% → 来点上手型内容；
  对 Y 兴趣高且正确率 95% → 来点硬的」。
- **为什么是它**：现在的推荐只关心"你爱看什么"，这是第一个关心"你学会了什么"的机制。
  它让 SM-2 的成绩回流到精选环节，闭环成立。Lyrien 从"推荐引擎"变成真正的"导师"。
- **代价**：一个聚合函数 + Prompt 改两行。零新增 LLM 调用。
- **风险**：早期数据稀疏，正确率噪声大 → 加最小样本量阈值（某标签复习 <5 次不参评）。

### 2.2 填空闪卡（Cloze Deletion，原子化记忆）
- **吃什么数据**：`notes` 表里的深挖教程。
- **做什么**：深挖完成后顺手让 LLM 把教程拆成 3~5 张填空卡
  （"slab 分配器通过 ____ 减少内存碎片"），存 `cloze_cards` 表，SM-2 三元组直接复用
  `review.py` 的逻辑。复习页里和整卡混排。
- **为什么**：现在的复习是"整卡自评"，粒度太粗 —— 一张卡四个知识点，一个不会就整卡重来。
  Anki 二十年验证过的结论：原子化才记得住。
- **代价**：新表 + 深挖时多一次小调用（可以异步做，不挡流式）。
- **风险**：LLM 出的填空卡质量参差 → 生成时给"一空一卡、答案在文中"的硬约束，
  差卡靠 👎 自然淘汰。

### 2.3 月报（周报的自然递归）
- **吃什么数据**：`reports` 表本身 + `tag_snapshots` 的月度序列。
- **做什么**：每月 1 号把上月周报喂给 LLM 做元复盘：哪些周报里立下的 flag 倒了、
  兴趣漂移的月度曲线、以及"知识缺口"分析（你的目标是 C++/网安/统计学，这个月实际学了什么）。
- **为什么**：周报看节奏，月报看方向。tag_snapshots 攒够 4 周就有了第一条真实的
  认知漂移曲线 —— 这是 plan.md 里"兴趣星图"的诚实形态（不画没有的数据）。
- **代价**：复用 `report.py` 八成功用，一次月度 LLM 调用。
- **风险**：无，数据天然在。

### 2.4 信源自动淘汰与发现
- **吃什么数据**：`sources.weight` 已有 👍/👎 增减。
- **做什么**：两件事。(a) 淘汰：信誉分跌破阈值的信源每周报告里点名"建议停用"，
  一键 disable（不自动删，留后悔药）。(b) 发现：统计你 👍 卡片的原始 URL 域名分布，
  周报里推荐"你常赞的 xx.com 有 RSS，要不要加"。
- **为什么**：Garbage In, Garbage Out 是 plan 三支柱之首，但现在信源只会增不会减，
  久了必然劣化。
- **代价**：纯 SQL 聚合 + 周报 prompt 加一节。
- **风险**：👍 样本少时误杀 → 只建议不自动执行。

### 2.5 成本仪表盘（让自己看得起自己）
- **吃什么数据**：现在 LLM 调用是黑箱。
- **做什么**：`llm.py` 每次调用顺手记 `usage`（token 数）进 `llm_usage` 表，
  周报末尾附一行"本周 AI 成本 ¥0.31，其中深挖占 60%"。
- **为什么**：个人项目最容易失控的就是隐性成本；量化之后，"要不要给某功能加 LLM 调用"
  就有了决策依据。而且看着每天几分钱跑出第二大脑，很爽。
- **代价**：`chat_completion*` 三个函数各加几行 + 一张表。

---

## 三、第二梯队：学习体验深化

### 3.1 费曼模式（最有教育含金量的一件）
- **做什么**：卡片上新增「讲给我听」按钮 → 你用自己的话写一段解释 →
  LLM 拿着原文当评委，流式点评：哪里讲对了、哪里是误解、哪个关键点漏了。
- **为什么**：主动回忆 > 被动重读，这是学习科学的头牌结论。SM-2 管"什么时候复习"，
  费曼管"复习时发生什么"。
- **代价**：一个新端点 + 一个 prompt（流式，复用 `streamInto`）。一次调用几分钱，按需触发。
- **风险**：评价太宽容 → prompt 里给评分 rubric 并要求"至少指出一个可以更好的点"。

### 3.2 晨间推送（把拉取变推送）
- **做什么**：每日管线跑完后，调一个 Webhook（Server酱 / Telegram Bot / Bark），
  把今日 4 张卡的标题 + 链接推到你手机。
- **为什么**：现在需要你"想起来打开"，推送让它真正占领早晨。
  这是打开率从 70% 到 99% 的那类改动。
- **代价**：`jobs.py` 管线末尾加 10 行 httpx 调用 + 两个环境变量。无 LLM 成本。
- **风险**：无，失败静默即可。

### 3.3 周测验（plan 创意储备池原生项）
- **做什么**：周日随周报附赠 5 道题（quiz.md 已验证过的模式，输入换成"本周全部卡片"），
  作答后 LLM 判分并写入 feedback。周报里多一行"本周测验 4/5"。
- **为什么**：给周报从"看"变成"考"，和 2.1 的掌握度地图互为数据源。
- **代价**：周报生成时多一次调用。

### 3.4 知识图谱可视化（第二大脑的"形状"）
- **吃什么数据**：notes ↔ cards ↔ tags 的共享关系（`related_notes` 已经在算）。
- **做什么**：`/graph` 页面，vis-network CDN 零构建渲染力导向图：
  标签是枢纽节点，笔记挂在标签上。孤立的笔记点一眼可见 → 提示"这里值得再深挖一篇"。
- **为什么**：它不改变任何数据，但改变你*看待*数据的方式 —— 知识网络的稀疏区
  就是下一步学习的方向。防"攒了一堆笔记却不成体系"。
- **代价**：纯前端 + 一个 JSON 端点，无 LLM。
- **风险**：节点 >500 时性能 → 届时再聚合，现在远不到。

---

## 四、第三梯队：输入与形态扩展

### 4.1 书签投喂（5 分钟工程，每天省 10 秒）
- **做什么**：一个 bookmarklet：`javascript:location.href='http://127.0.0.1:8000/?feed='+encodeURIComponent(location.href)`。
  首页支持 `?feed=` 参数自动填入投喂框。在浏览器里看到好文 → 点书签 → 回 Lyrien 确认。
- **为什么**：投喂功能已验证，这是它的摩擦力最小化。
- **代价**：index 路由读一个 query 参数，预填 input。就这么多。

### 4.2 YouTube/播客信源
- **做什么**：`sources` 表加 `type='youtube'`，用 yt-dlp 或 YouTube 字幕 API 拉字幕，
  走同一管线（字幕即"全文"）。
- **为什么**：技术内容越来越视频化，只读 RSS 等于自断一臂。
- **代价**：新依赖 + 字幕质量参差（无字幕/机翻字幕要降级处理）。
  建议只在你真的常看技术视频时再做。

### 4.3 PWA（通勤场景）
- **做什么**：manifest + 一个极简 service worker，缓存今日卡片页静态资源。
  桌面/手机加图标，地铁上能看今早的卡片。
- **为什么**：目前唯一的移动场景缺口。
- **代价**：半天工程，无 LLM。注意 service worker 别缓存动态接口（复习评分会出鬼）。

### 4.4 Anki 导出（如果你本来就用 Anki）
- **做什么**：`/export/anki` 导出掌握队列卡片为 CSV（正面=引导思考题，背面=核心概念+链接）。
- **为什么**：有人就爱在 Anki 里复习。但如果你只用 Lyrien 复习，这就是冗余。
- **代价**：一个 SELECT + CSV 响应，极便宜。做不做取决于你有没有 Anki 习惯。

---

## 五、我明确不建议现在做的

| 创意 | 为什么不 |
|---|---|
| 🤝 双人模式 | 单人自用是 plan 的第一原则。一上多用户，认证、隔离、部署全来了，回报率是负的。 |
| ✂️ 笔记自动合并/修剪 | 几百篇量级下人工修剪 10 分钟搞定，让 LLM 自动删改知识库是拿精确性换懒惰。 |
| 向量问答（sqlite-vec） | 已决策过：DeepSeek 无 embeddings。FTS5 召回在当前量级够用，等问题真实出现再升级。 |
| Tauri 套壳 | PWA 覆盖 90% 的"桌面图标"诉求，成本是 Tauri 的 5%。 |
| 移动端原生 App | 同上，PWA 先到，原生永远不急。 |
| 更复杂的推荐算法（协同过滤等） | 数据量级下，规则 + LLM 精选已过剩。别把个人工具做成论文。 |

---

## 六、如果只让我挑三件做

1. **2.1 掌握度感知推荐** —— 它让"复习"和"推荐"两个子系统第一次咬合，
   这是从"懂你兴趣"到"教你学会"的质变，而且几乎免费。
2. **3.2 晨间推送** —— 十行代码换打开率，所有后续功能都依赖"你每天真的会来"。
3. **2.2 填空闪卡** —— 把复习粒度从"卡"打到"知识点"，记忆效率的杠杆最大。

三件做完，Lyrien 的闭环就是完整的：
**抓取 → 精选（兴趣+掌握度双感知）→ 学习 → 原子化记忆 → 复习 → 成绩回流精选**。
再往后，就是每月看一眼月报，确认自己在变聪明。

---

## 七、一个元建议

这个项目的创意来源，最好的不是我，是你的**使用摩擦**。
我的建议是给 README 留一个"摩擦日志"小节：每天用的时候，哪里让你皱了一下眉、
哪里让你心想"要是能…就好了"，随手记一行。攒两周，那就是比本文档值钱十倍的创意池 ——
因为它来自真实需求，而不是我的推理。
