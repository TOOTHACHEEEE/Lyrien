# Lyrien — 个人 AI 学习引擎 · 项目计划书

> 一句话愿景：**每天早上打开它，3~5 张为你量身定制的高质量学习卡片已经躺在那里；你只管学，抓取、筛选、总结、复习提醒，全部由后台默默完成。**

---

## 一、对初稿的分析：方向对，但要做减法

原方案（Tauri + React + FastAPI + ChromaDB + Celery/APScheduler）思路成熟，但对**单人自用项目**有三处过度设计，违背了"方便最重要"的第一原则：

| 初稿选型 | 问题 | 修正方案 |
|---|---|---|
| Tauri 桌面端 | 多一条 Rust 构建链，开发/打包/调试成本翻倍 | **先用本地 Web 应用**：浏览器打开 `localhost` 就是毫秒级，后期想要桌面图标再用 PWA 或 Tauri 套壳 |
| ChromaDB | 独立向量数据库，重 | 个人知识库量级（几百到几千条）前期用 **SQLite FTS5 全文检索**足够；第二阶段再上 **sqlite-vec**（单文件向量扩展，零服务） |
| Celery | 需要额外跑 Redis | **APScheduler** 内嵌在后端进程里即可 |

**初稿中完全保留的精华：**
- 后台异步预计算 → 打开秒看（这是整个架构的灵魂，不动摇）
- 两阶段过滤（LLM 当过滤器，不当生成器）
- 👍/👎 反馈闭环更新兴趣权重
- 严格 Prompt 控制输出质量

**初稿漏掉、本计划补上的关键点：**
1. **笔记本凌晨 3 点是关机的** —— 定时任务必须有"启动时补跑"逻辑：应用启动时检查今天的卡片是否已生成，没有则后台补跑。
2. **只看 RSS 摘要，总结质量必然差** —— 必须抓全文（用 `trafilatura` 提取正文）再喂给 LLM。
3. **防幻觉硬约束** —— 卡片必须附原始链接，Prompt 中明令"只允许基于给定正文总结，禁止补充文中没有的事实"。
4. **学习 ≠ 看过** —— 初稿没有复习机制，看过就忘。本计划加入**简化版间隔重复（SM-2）**。

---

## 二、最终效果预览（建成后的使用画面）

**早晨 8:00** —— 双击桌面图标（或浏览器书签），页面瞬间打开（<200ms）。今日 3~5 张卡片已生成好：

```
┌─────────────────────────────────────────────┐
│ 📄 Linux 内核引入新的 slab 分配器防护机制      │
│ 🏷️ 网络安全 · 内存管理          信源: LWN ⭐⭐⭐ │
│                                             │
│ 【核心概念】...                               │
│ 【为什么值得学】这直接关联你上周学过的          │
│   「C++ 指针边界检查」，是同一问题在            │
│   内核态的对策 → 建议复习笔记 #42             │
│ 【引导思考】如果这种溢出发生在 Docker           │
│   容器里，与宿主机有何不同？                   │
│                                             │
│ [🔍 深挖成教程]  [👍] [👎] [💡 已掌握]         │
└─────────────────────────────────────────────┘
```

- 点 **🔍 深挖**：LLM 基于全文流式生成一篇完整教程，自动存入知识库，并链接到相关旧笔记。
- 点 **👍/👎**：实时更新你的兴趣标签权重和该信源的信誉分。明天的推荐立刻更准。
- 每 5 张卡里约有 1 张 **🎲 探索卡**：来自你兴趣圈之外的高质量内容（防信息茧房，对应你说的"挖掘不相干的内容"）。

**晚上 21:00** —— 复习页提醒你：3 天前标记"已掌握"的卡片到了复习时点，出几个问题考考你；答得好，下次复习间隔拉长。

**周日晚** —— 自动生成**学习周报**：本周学了什么、兴趣画像漂移趋势、信源质量排行、下周建议方向。

---

## 三、技术架构（减法版）

```
┌──────────────────────────────────────────────┐
│  前端：Jinja2 + HTMX + Tailwind(CDN)          │
│  —— 零构建步骤，FastAPI 直接渲染，一个进程      │
├──────────────────────────────────────────────┤
│  后端：Python 3.11+ / FastAPI                 │
│  ├── 路由层：卡片/反馈/复习/知识库/设置        │
│  ├── pipeline/：抓取→去重→初筛→精选→总结      │
│  └── APScheduler：每日定时 + 启动时补跑        │
├──────────────────────────────────────────────┤
│  数据：SQLite（单文件）                        │
│  ├── 一阶段：FTS5 全文检索                     │
│  └── 二阶段：sqlite-vec 向量关联（按需启用）    │
├──────────────────────────────────────────────┤
│  LLM：OpenAI 兼容接口                          │
│  —— 默认 DeepSeek（便宜、中文好），            │
│    改一行 base_url 即可切到 Claude/Ollama     │
└──────────────────────────────────────────────┘
```

**目录结构：**

```
lyrien/
├── plan.md                  ← 本文件
├── pyproject.toml
├── .env                     ← LLM_API_KEY 等（不提交）
├── app/
│   ├── main.py              # FastAPI 入口 + 路由
│   ├── config.py            # 读取 .env
│   ├── db.py                # SQLite 连接 + 建表
│   ├── llm.py               # OpenAI 兼容客户端（含流式）
│   ├── pipeline/
│   │   ├── fetch.py         # RSS/API 抓取 + trafilatura 提取全文
│   │   ├── filter.py        # 两阶段过滤 + 探索卡注入
│   │   ├── summarize.py     # 卡片生成
│   │   └── jobs.py          # APScheduler 任务 + 启动补跑
│   ├── feedback.py          # 标签权重 + 信源信誉更新
│   ├── notes.py             # 知识库：笔记保存 / CJK 分词 FTS5 搜索 / 关联
│   ├── review.py            # 简化 SM-2 间隔重复
│   ├── prompts/             # 所有 Prompt 独立成文件，方便迭代
│   ├── templates/           # Jinja2
│   └── static/
├── data/                    # lyrien.db（gitignore）
└── tests/
```

**核心数据表（第一版）：**

```sql
sources   (id, name, type, url, weight, enabled)          -- 信源+信誉分
items     (id, source_id, title, url, fulltext, fetched_at) -- 抓取的原文
cards     (id, date, item_id, title, summary_json, tags,    -- 每日卡片
           is_explore, status, ease, interval, due_date)    -- 后三列给复习用
feedback  (id, card_id, action, created_at)                -- 👍/👎/💡
tags      (id, name, weight, updated_at)                   -- 兴趣画像
notes     (id, title, content, card_id, created_at)        -- 深挖教程/知识库
```

---

## 四、核心机制设计

### 4.1 每日管线（夜间自动 + 启动补跑）

```
抓取 → 去重 → 初筛(便宜) → 精选(强LLM) → 抓全文 → 生成卡片 → 入库
 ①        ②        ③            ④           ⑤          ⑥
```

1. **抓取**：`feedparser` 拉取所有启用信源近 24h 的新条目（目标 ~100 条）。
2. **去重**：URL 哈希 + 标题相似度。
3. **初筛（极低成本）**：规则 + 兴趣标签关键词匹配，100 → 15 条。
4. **精选（强 LLM）**：15 条标题+摘要喂给 LLM，按严格 Prompt 选出 3~5 条，**并强制注入 1 条探索卡**（从兴趣圈外的信源池抽）。
5. **抓全文**：`trafilatura` 提取正文，失败的降级用摘要。
6. **生成卡片**：逐条调用 LLM 产出结构化 JSON（核心概念/为什么值得学/关联旧知识/引导问题），入库。

**启动补跑**：`main.py` 启动时检查 `cards.date == today` 是否存在；不存在 → 后台异步跑管线，前端显示"今天的卡片正在烹饪中 🍳"，完成后自动刷新。

### 4.2 质量保证三支柱

- **Garbage In, Garbage Out**：信源白名单制。初始推荐：Hacker News、arXiv(cs.CR/math/cs.SE)、LWN、你信任的博客 RSS。每个信源有信誉分，👎 多的信源自动降权。
- **LLM 是过滤器不是生成器**：推荐内容必须来自真实抓取的条目；卡片生成必须基于抓到的全文；输出必须带原始链接。Prompt 中写明"禁止编造正文中没有的事实，禁止营销号腔调"。
- **利用 vs 探索（80/20）**：80% 卡片贴合当前兴趣权重，20% 探索卡来自圈外高质量信源，防止推荐系统越用越窄。

### 4.3 反馈闭环（简化版强化学习）

- 点击 👍：卡片所有标签 `weight += 0.1`；信源 `weight += 0.05`
- 点击 👎：标签 `weight -= 0.15`；信源 `weight -= 0.1`
- 点击 💡 已掌握：标签权重不变，卡片进入**复习队列**（SM-2：1天后→3天后→7天后→指数拉长）
- 每次精选时，把 Top-8 权重标签写进 Prompt："用户近期对 X、Y 表现出高兴趣"
- 权重每周做一次归一化 + 衰减（不互动的兴趣自然降温）

### 4.4 速度保障（响应你的第一需求）

| 场景 | 策略 | 目标 |
|---|---|---|
| 打开应用看今日卡片 | 全部预计算，一句 `SELECT` | <200ms |
| 深挖教程 / 自由提问 | SSE 流式输出（打字机效果） | 首字 <1s |
| 后台管线 | 全异步，与前端完全隔离 | 无感知 |
| 前端 | 零构建，无打包等待 | 改完即刷 |

### 4.5 成本控制

每日 LLM 调用有界：精选 1 次（~15 条摘要输入）+ 卡片生成 ~5 次。用 DeepSeek 量级约为**每天几分钱**。初筛坚决不用大模型。

---

## 五、施工路线图（四个阶段，每阶段可独立验收）

### Phase 1 · 最小可用闭环（第 1~3 天）
> 目标：打开页面能看到今天的卡片。

- 初始化项目（pyproject + FastAPI + SQLite 建表）
- 手写 3 个 RSS 信源，实现抓取 + 去重 + 入库
- 实现 LLM 客户端 + 单阶段精选 + 卡片生成
- 一个极简页面展示今日卡片（Jinja2 + Tailwind CDN）
- ✅ 验收：跑一次 `python -m app.pipeline.jobs`，刷新页面看到 ≥3 张带链接的卡片

### Phase 2 · 质量与反馈（第 4~7 天）
> 目标：推荐开始"越用越懂你"。

- trafilatura 全文提取接入管线
- 两阶段过滤 + 探索卡机制
- 👍/👎/💡 按钮 + 标签/信源权重更新
- APScheduler 每日任务 + 启动补跑逻辑
- ✅ 验收：连续用 3 天，👍 率呈上升趋势；关机错过定时后，次日打开能自动补跑

### Phase 3 · 知识库与复习（第 2 周）✅ 已完成
> 目标：从"资讯推荐器"升级为"第二大脑"。

- 🔍 深挖：卡片 → 流式生成完整教程 → 存入 notes（幂等：同卡不重复深挖；流末哨兵 `__NOTE_SAVED__:<id>` 回传笔记 ID）
- 知识库页面 `/kb`：笔记列表 + FTS5 搜索（CJK 字级分词，见 notes.py）+ 笔记详情页按共享标签做新旧知识关联展示
- 简化 SM-2 复习队列 + 每日复习页 `/review`：三档自评（忘记/模糊/记得）+ 卡片自带引导思考题 + AI 流式出题
- ✅ 验收：能对一张卡片深挖并搜到教程；3 天前的卡片准时出现在复习页

### Phase 4 · 打磨与扩展（第 3 周起，按需挑选）
- 📥 手动投喂：粘贴任意 URL，走同一管线生成卡片 ✅（manual 信源 + 异步状态轮询，不占每日配额）
- 💬 对自己知识库的 RAG 问答 ✅（调整为 RAG-lite：DeepSeek 无 embeddings，FTS5 召回 + LLM 引用作答；sqlite-vec 留待有 embedding 模型时）
- 📊 学习周报 ✅（周日晚 21 点定时 + 启动补跑 + 手动生成；兴趣画像漂移靠 tag_snapshots 周快照）
- 🕰️ 知识考古 ✅（复习页随机翻出 7 天前旧笔记）
- ✍️ 内置 Markdown 编辑器 ✅（手动笔记 + 标签参与关联/搜索 + localStorage 草稿防丢）
- ⬜ ✂️ 知识库自动修剪：合并相似旧笔记
- ⬜ 📱 PWA 化（加桌面图标）或 Tauri 套壳
- ⬜ 🧪 周测验：LLM 根据本周卡片出 5 道小测验
- ⬜ 🎙️ 播客/视频信源：抓 YouTube 字幕走同一管线

---

## 六、第一版精选 Prompt 草案（放入 `prompts/select.md` 迭代）

```
你是一位严谨的导师，为一名正在学习 C++、网络安全、统计学的大二学生筛选学习内容。

用户近期高权重兴趣标签：{top_tags}

请从以下 {n} 条资讯中选出最多 4 条最值得学习的，并额外从【探索池】中选 1 条
与上述兴趣无关但质量极高的内容（探索卡）。

评判标准：
1. 与用户现有知识有潜在联系，或提供全新的高质量视角
2. 有信息密度：涉及原理、机制、新方法；拒绝营销、软文、浅层资讯
3. 优先一手信源（官方博客、论文、文档）

严格按 JSON 输出，不要输出任何其他内容：
[{"index": 0, "is_explore": false, "reason": "一句话说明为什么选它"}]
```

---

## 七、风险与注意

- **API Key 安全**：只放 `.env`，gitignore，代码里永不硬编码。
- **RSS 失效**：抓取层对每个信源独立 try/except，单个信源挂掉不影响管线。
- **LLM 输出非法 JSON**：`llm.py` 里做一次解析失败自动重试（可复用你 llm_repair 的修复思路）。
- **不要过早优化**：Phase 1 不需要向量、不需要用户系统、不需要 Tauri。忍住。

---

## 八、创意储备池（做好核心后，随手挑一个加）

- 🕰️ **知识考古**：随机翻出一张半年前的笔记问你"还记得吗"
- 🧪 **出题模式**：LLM 根据本周卡片出 5 道小测验
- 🎙️ **播客/视频信源**：抓 YouTube 字幕走同一管线
- 🗺️ **兴趣星图**：标签权重随时间变化的曲线图，看你的认知漂移
- 🤝 **双人模式**（远期）：导出信源信誉分给朋友，互换高质量信源
